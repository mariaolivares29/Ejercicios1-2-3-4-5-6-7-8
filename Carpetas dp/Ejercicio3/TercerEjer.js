let Pi = 3.1416
let Radio = Number(prompt("Calcula la superficie de un circulo\nDigite el valor del Radio"));

let superficie = Pi * Radio**2

document.write("La superficie del circulo es : ",superficie);